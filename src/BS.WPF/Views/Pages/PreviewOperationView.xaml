<Page x:Class="BS.WPF.Views.Pages.PreviewOperationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BS.WPF.Views.Pages"
      xmlns:viewModels="clr-namespace:BS.Vms.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance viewModels:PreviewOperationViewModel}"
      Title="PreviewOperationView">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Price" Command="{Binding GetPriceAsyncCommand, Mode=OneWay}"  Height="75" Width="200"></Button>
            <Button Content="Contact" Command="{Binding GetContactsAsyncCommand, Mode=OneWay}" Height="75" Width="200"></Button>
            <Button Content="Contact" Command="{Binding GetClientsAsyncCommand, Mode=OneWay}" Height="75" Width="200"></Button>
            </StackPanel>
        
        
    </Grid>
</Page>
